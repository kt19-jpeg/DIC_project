# Model Training

This folder contains the ML training pipeline for the cleaned overdose dataset.

## Goals

- Train 3 regression algorithms on the processed dataset.
- Use chronological train/validation/test splits.
- Save comparable metrics and result artifacts in `results/model_training`.

## Run

```bash
python model_training/train_models.py
```
